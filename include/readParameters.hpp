#ifndef HH_READPARAMETERS_HH
#define HH_READPARAMETERS_HH
#include <string>
#include <parameters.hpp>
//read the file with the parameters (json fornat only)
parameters readParameters(std::string const &filename);
#endif